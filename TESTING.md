

1. Batch run - evaluating models
2. In moodle - final user test
3. CLI tests
    - dump mode (reparsing)
    - moodle/sandbox mode (sandbox)

We can probably skip dump mode, as the dump/reparse is
increasingly simple, and it is tested also in the sandbox.
